<%= content_tag :div, class: class_names("card", @options.delete(:class)), **@options do %>
  <%= header %>

  <% if body? %>
    <div class="card__body">
      <%= body %>
    </div>
  <% end %>

  <%= footer %>
<% end %>
